(()=>{const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>'\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':'&quot;'}[c])),idr=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(v)||0);let page=1,totalPages=1,timer;
const key=(p,q,c)=>`kia_catalog_v051_${p}_${c}_${q.toLowerCase().slice(0,80)}`;
function read(k){try{const x=JSON.parse(localStorage.getItem(k)||'null');return x&&Date.now()-x.t<6*3600000?x.d:null}catch{return null}}
function write(k,d){try{localStorage.setItem(k,JSON.stringify({t:Date.now(),d}))}catch{}}
function card(p){const media=p.media||[],coverMedia=media.find(x=>x.is_cover)||media[0],cover=coverMedia?.public_url||p.cover_image_url||'',pct=Math.min(100,Math.round((Number(p.raised_amount)||0)/(Number(p.target_amount)||1)*100)),other=media.filter(x=>x!==coverMedia),thumbs=other.slice(0,3).map(x=>`<img class="program-thumb" loading="lazy" decoding="async" src="${esc(x.thumbnail_url||x.public_url)}" alt="">`).join(''),extra=Math.max(0,other.length-3);return `<article class="card public-program"><div class="program-poster" style="background-image:url('${esc(cover)}')"></div>${media.length>1?`<div class="program-thumbs">${thumbs}${extra?`<span class="program-more">+${extra}</span>`:''}</div>`:''}<div class="program-body"><span class="program-category">${esc(p.category)}</span><h3>${esc(p.program_name)}</h3><div class="progress"><span style="width:${pct}%"></span></div><div class="program-meta"><span>${idr(p.raised_amount)} / ${idr(p.target_amount)}</span><span>${pct}%</span></div><a class="btn btn-primary" style="width:100%" href="./program.html?id=${encodeURIComponent(p.program_id)}">Lihat Program</a></div></article>`}
function render(data){const root=$('[data-catalog-grid]');totalPages=data.total_pages||1;page=data.page||1;root.innerHTML=(data.items||[]).map(card).join('')||'<div class="empty-state" style="grid-column:1/-1">Program tidak ditemukan.</div>';$('[data-catalog-page]').textContent=`${page} / ${totalPages}`;$('[data-catalog-prev]').disabled=page<=1;$('[data-catalog-next]').disabled=page>=totalPages}
async function request(url,timeout){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{signal:c.signal});const j=await r.json();if(!r.ok||!j.success)throw new Error(j.message||'CATALOG_FAILED');return j.data}finally{clearTimeout(t)}}
async function load(next=1){page=next;const root=$('[data-catalog-grid]'),q=$('[data-catalog-search]').value.trim(),cat=$('[data-catalog-category]').value,k=key(page,q,cat),cached=read(k);if(cached)render(cached);else root.innerHTML='<div class="skeleton skeleton-card"></div><div class="skeleton skeleton-card"></div><div class="skeleton skeleton-card"></div>';try{const data=await request(`${KIA_CONFIG.BACKEND_URL}/api/public/programs?page=${page}&limit=12&search=${encodeURIComponent(q)}&category=${encodeURIComponent(cat)}`,cached?8000:12000);write(k,data);render(data)}catch(e){if(!cached)root.innerHTML='<div class="empty-state" style="grid-column:1/-1">Program belum dapat dimuat. <button class="btn btn-ghost" type="button" data-catalog-retry>Coba Lagi</button></div>';$('[data-catalog-retry]')?.addEventListener('click',()=>load(page))}}
document.addEventListener('DOMContentLoaded',()=>{$('[data-catalog-prev]').onclick=()=>page>1&&load(page-1);$('[data-catalog-next]').onclick=()=>page<totalPages&&load(page+1);$('[data-catalog-category]').onchange=()=>load(1);$('[data-catalog-search]').oninput=()=>{clearTimeout(timer);timer=setTimeout(()=>load(1),320)};load(1)});})();
