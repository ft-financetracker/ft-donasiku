(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':'&quot;'}[c])),idr=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(v)||0);let program=null,media=[],active=0;
function imageUrl(m){return m?.public_url||m?.thumbnail_url||program?.cover_image_url||''}
function render(){const root=$('[data-program-root]');if(!program)return;media=Array.isArray(program.media)?program.media:[];const coverIndex=Math.max(0,media.findIndex(x=>x.is_cover));active=coverIndex>=0?coverIndex:0;const pct=Math.min(100,Math.round((Number(program.raised_amount)||0)/(Number(program.target_amount)||1)*100));root.innerHTML=`<div class="program-breadcrumb"><a href="./programs.html">Semua Program</a><span>›</span><span>${esc(program.category)}</span></div><div class="program-layout"><div><div class="program-gallery"><img class="program-main-image" data-main-image src="${esc(imageUrl(media[active]))}" alt="${esc(program.program_name)}">${media.length>1?`<div class="program-thumb-row">${media.map((m,i)=>`<button class="program-thumb-btn ${i===active?'is-active':''}" data-thumb-index="${i}"><img src="${esc(m.thumbnail_url||m.public_url)}" alt=""></button>`).join('')}</div>`:''}</div><div class="program-content"><span class="program-category">${esc(program.category)}</span><h1>${esc(program.program_name)}</h1><p class="program-description">${esc(program.description||program.short_description||'')}</p></div></div><aside class="card donation-card" id="donasi"><h3>Dukung Program Ini</h3><p class="muted mini">Donasi tervalidasi</p><div class="donation-amount">${idr(program.raised_amount)}</div><div class="progress"><span style="width:${pct}%"></span></div><div class="progress-meta"><span>${pct}% tercapai</span><span>Target ${idr(program.target_amount)}</span></div><div class="program-actions"><button class="btn btn-primary" type="button" data-donate>Donasi Sekarang</button><button class="btn btn-ghost" type="button" data-share>Bagikan Program</button></div><p class="program-note">Pembayaran online akan menggunakan alur payment gateway KIA. Fitur checkout disiapkan pada tahap integrasi pembayaran.</p></aside></div>`;$$('[data-thumb-index]').forEach(b=>b.onclick=()=>selectImage(Number(b.dataset.thumbIndex)));$('[data-main-image]')?.addEventListener('click',openLightbox);$('[data-share]')?.addEventListener('click',share);$('[data-donate]')?.addEventListener('click',()=>alertCheckout())}
function selectImage(i){active=i;const main=$('[data-main-image]');if(main)main.src=imageUrl(media[i]);$$('[data-thumb-index]').forEach(b=>b.classList.toggle('is-active',Number(b.dataset.thumbIndex)===i))}
function openLightbox(){if(!media.length)return;$('[data-lightbox-image]').src=imageUrl(media[active]);$('[data-lightbox]').hidden=false}
function alertCheckout(){const el=$('[data-donate]');el.textContent='Checkout segera tersedia';setTimeout(()=>el.textContent='Donasi Sekarang',1600)}
async function share(){const url=location.href,text=(program.share_message||`Lihat program ${program.program_name} di KIA.`).trim();try{if(navigator.share)await navigator.share({title:program.program_name,text,url});else{await navigator.clipboard.writeText(`${text}\n\n${url}`);const b=$('[data-share]');b.textContent='Tersalin';setTimeout(()=>b.textContent='Bagikan Program',1500)}}catch(_){}}
async function load(){const id=new URLSearchParams(location.search).get('id');if(!id){$('[data-program-root]').innerHTML='<div class="empty-state">Program tidak ditemukan.</div>';return}try{const r=await fetch(`${KIA_CONFIG.BACKEND_URL}/api/public/programs/${encodeURIComponent(id)}`,{cache:'no-store'}).then(x=>x.json());if(!r.success)throw new Error(r.message);program=r.data.program;render()}catch(e){$('[data-program-root]').innerHTML=`<div class="empty-state">${esc(e.message||'Program belum dapat dimuat.')}</div>`}}
document.addEventListener('DOMContentLoaded',()=>{$('[data-lightbox-close]').onclick=()=>{$('[data-lightbox]').hidden=true};$('[data-lightbox]').onclick=e=>{if(e.target.matches('[data-lightbox]'))e.currentTarget.hidden=true};load()});})();
