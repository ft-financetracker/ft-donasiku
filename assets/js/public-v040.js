(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)],idr=v=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(v)||0),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':'&quot;'}[c]));let heroes=[],heroIndex=0;
function renderHero(){const h=heroes[heroIndex];if(!h)return;$('[data-hero-title]').textContent=h.title;$('[data-hero-subtitle]').textContent=h.subtitle;const img=$('[data-hero-image]');img.style.backgroundImage=`url("${h.image_url}")`;const cta=$('[data-hero-cta]');cta.textContent=h.cta_label||'Mulai Berdonasi';cta.href=h.cta_url||'#program';$('[data-hero-dots]').innerHTML=heroes.map((_,i)=>`<button class="hero-dot ${i===heroIndex?'is-active':''}" data-dot="${i}" aria-label="Hero ${i+1}"></button>`).join('');$$('[data-dot]').forEach(b=>b.onclick=()=>{heroIndex=Number(b.dataset.dot);renderHero()})}
function thumbs(p){const m=p.media||[],cover=m.find(x=>x.is_cover),rest=m.filter(x=>x!==cover).slice(0,3),extra=Math.max(0,m.length-(cover?1:0)-rest.length);return rest.map(x=>`<img class="program-thumb" src="${esc(x.thumbnail_url||x.public_url)}" data-thumb="${esc(x.public_url)}" alt="">`).join('')+(extra?`<span class="program-more">+${extra}</span>`:'')}
function renderPrograms(items){const root=$('[data-public-programs]');if(!items.length){root.innerHTML='<div class="empty-state" style="grid-column:1/-1">Belum ada program aktif.</div>';return}root.innerHTML=items.map(p=>{const media=p.media||[],cover=(media.find(x=>x.is_cover)||media[0])?.public_url||p.cover_image_url||'',pct=Math.min(100,Math.round((Number(p.raised_amount)||0)/(Number(p.target_amount)||1)*100));return `<article class="card public-program"><div class="program-poster" style="background-image:url('${esc(cover)}')" data-poster></div>${media.length>1?`<div class="program-thumbs">${thumbs(p)}</div>`:''}<div class="program-body"><span class="program-category">${esc(p.category)}</span><h3>${esc(p.program_name)}</h3><div class="progress"><span style="width:${pct}%"></span></div><div class="program-meta"><span>${idr(p.raised_amount)} terkumpul</span><span>${pct}%</span></div><a class="btn btn-primary" style="width:100%" href="./program.html?id=${encodeURIComponent(p.program_id)}">Lihat Program</a></div></article>`}).join('');$$('[data-thumb]',root).forEach(t=>t.onclick=()=>{const card=t.closest('.public-program');card.querySelector('[data-poster]').style.backgroundImage=`url('${t.dataset.thumb}')`})}
function renderFaq(items){const root=$('[data-faq-preview]');root.innerHTML=(items||[]).slice(0,4).map(x=>`<details><summary>${esc(x.question)}</summary><p>${esc(x.answer)}</p></details>`).join('')}
async function load(){try{const r=await fetch(`${KIA_CONFIG.BACKEND_URL}/api/public/bootstrap`,{cache:'no-store'}).then(x=>x.json());if(!r.success)throw new Error(r.message);heroes=(r.data.heroes||[]);if(!heroes.length)heroes=[{title:'Kecil di Tangan Kita, Besar untuk Mereka',subtitle:'Setiap kebaikan dapat menjadi harapan baru.',cta_label:'Mulai Berdonasi',cta_url:'#program',image_url:'./assets/images/hero/hero-1.png'}];renderHero();renderPrograms(r.data.programs||[]);renderFaq(r.data.faqs||[]);$('[data-stat-paid]').textContent=idr(r.data.stats?.total_paid_amount);$('[data-stat-programs]').textContent=String(r.data.stats?.active_programs||0);$('[data-stat-disbursed]').textContent=idr(r.data.stats?.total_withdrawn_net)}catch(e){$('[data-public-programs]').innerHTML='<div class="empty-state" style="grid-column:1/-1">Program belum dapat dimuat. Silakan coba lagi.</div>'}}
document.addEventListener('DOMContentLoaded',()=>{$('[data-hero-prev]').onclick=()=>{heroIndex=(heroIndex-1+heroes.length)%heroes.length;renderHero()};$('[data-hero-next]').onclick=()=>{heroIndex=(heroIndex+1)%heroes.length;renderHero()};load()});})();
